const aria=process.argv.slice(2).join(' ');const pages=await fetch('http://127.0.0.1:9231/json').then(r=>r.json());const page=pages.find(p=>p.type==='page'&&p.url.includes('run=file-e2e'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async e=>(await c('Runtime.evaluate',{expression:e,returnByValue:true})).result.result.value;const r=await v(`(()=>{const find=root=>{for(const e of root.querySelectorAll('*')){if(e.getAttribute?.('aria-label')===${JSON.stringify(aria)})return e;if(e.shadowRoot){const x=find(e.shadowRoot);if(x)return x}}};const e=find(document);if(!e)return null;const r=e.getBoundingClientRect();return{x:r.x+r.width/2,y:r.y+r.height/2}})()`);if(!r)throw new Error('not found '+aria);for(const type of ['mousePressed','mouseReleased'])await c('Input.dispatchMouseEvent',{type,x:r.x,y:r.y,button:'left',clickCount:1});console.log(JSON.stringify(r));ws.close();