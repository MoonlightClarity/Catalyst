# FORWARD_RESEARCH checkpoint — 2026-09-11

Role boundary: current FOSS comparator/source research only. Do not manipulate Catalyst GUI and do not implement product code.

Recovered coordination/environment/profile state. Catalyst remains on :99 / 5902 / 6080; live research jobs must not be disturbed. Current state file is `C:\Users\iris\AgentWorkbench\catalyst\state\current.json`.

Coordinator recovery target is exact: `Catalyst Rollover Research` at https://chatgpt.com/c/6aa43ff5-7900-83ea-86ff-fd03580fab20 . If it expires mid-sequence and Opera bridge access is validated, send exactly `continue`, then return to this role.

Completed bounded comparator: AFFiNE / BlockSuite.

Durable pass: `PASS-2026-09-11-forward-research-affine.md`.

Key AFFiNE result: Page and Edgeless documents are isomorphic with no conversion on mode switch; BlockSuite separates rich-text block hierarchy, free spatial geometry, surface elements, geometric frames, and explicit groups. This is strong implementation evidence for Catalyst's distinction among identity, structural containment, placement/geometry, and association.

Current stable AFFiNE release found: 0.27.4 (2026-08-18); 0.27.5 remains beta, with current canaries continuing.

Next bounded comparator: Logseq. Research block identity, references/transclusion, page-vs-block addressing, local persistence/database evolution, and implications for Catalyst multiple-occurrence/provenance semantics.

## Completed comparator: Logseq

Durable pass: `PASS-2026-09-11-forward-research-logseq.md`.

Key result: Logseq demonstrates stable block/node identity reused through references and editable embeds, with backlinks making reuse inspectable. Its 2.0 DB line also shows the tradeoff of unifying pages and blocks as nodes and of moving from Markdown-file graphs to SQLite-backed DB graphs.

Catalyst consequence: multiple occurrences should remain located portrayals of one identity; read-through occurrence, editable same-object occurrence, and transformed derivative should be distinct provenance states. Full-fidelity archival/export must be documented separately from lossy interoperability projections.

Next comparator: Excalidraw — geometry, element identity, bindings, grouping/frames, file format, and the boundary between spatial relation and semantic relation.

## Completed comparator: Excalidraw

Durable pass: `PASS-2026-09-11-forward-research-excalidraw.md`.

Key result: Excalidraw cleanly separates scene-element identity/geometry from grouping, frame membership, and connector bindings in a documented plaintext JSON scene model.

Catalyst consequence: semantic graph state and scene graph state should be separable. Connector portrayals may follow occurrence geometry, but routing/grouping/frame operations must not silently create or delete analytical truth.

Next comparator: Zettlr — file-first/open-format scholarship, citation integration, workspace/project boundaries, and document-centric strengths Catalyst should retain.

## Completed comparator: Zettlr

Durable pass: `PASS-2026-09-11-forward-research-zettlr.md`.

Key result: Zettlr demonstrates a strong file-first/open-format boundary: ordinary folders remain workspaces, projects are folder-scoped behavior, citations reuse standard external bibliography formats, and citation text can return to source PDFs.

Catalyst consequence: richer analytical state should not needlessly seize ownership of the source corpus. Bibliographic identity, exact source anchor, analytical use, and final citation rendering should remain separate layers.

Next: add another comparator only if it stresses a genuinely new mechanism rather than repeating block/database/canvas features already covered.

## Completed comparator: SilverBullet

Durable pass: `PASS-2026-09-11-forward-research-silverbullet.md`.

Key result: SilverBullet keeps canonical Markdown distinct from a rebuildable Object Index and exposes indexed interpretation through optional X-Ray/query projections. Its evolution toward stable `$name` anchors also highlights the weakness of page+position addressing for durable evidence identity.

Catalyst consequence: distinguish authoritative analyst/source state, reproducible derived state, and ephemeral presentation state. Lenses may expose computed interpretation without mutating truth; editable projections must retain durable identity references.

First bounded forward-research sweep is complete. Next step is cross-comparator synthesis before selecting any additional system.

## Completed comparator: Hypothesis / W3C Web Annotation anchoring

Durable pass: `PASS-2026-09-11-forward-research-hypothesis-anchoring.md`.

Key result: trustworthy source return should use redundant, format-aware selectors plus source-representation/version state. Hypothesis provides current FOSS implementation evidence for ordered selector fallback; W3C Web Annotation explicitly supports multiple selectors and resource states.

Catalyst consequence: source-derived objects need durable source identity, representation/version identity, one or more segment selectors, transformation provenance, and current resolution status. Fuzzy/recovered anchoring must be distinguishable from exact anchoring and covered by real-artifact regression fixtures.

Remaining high-value evidence gaps are now narrow: provenance-preserving transformation pipelines and multi-user conflict semantics. Avoid returning to generic note/canvas comparators unless they address one of these gaps.

## Completed comparator: OpenRefine transformation provenance

Durable pass: `PASS-2026-09-11-forward-research-openrefine-provenance.md`.

Key result: transformation specification, execution event, output/change, UI undo history, and archival history are different records. Replayable method definitions should not be confused with evidence that a method actually ran on a specific input.

Catalyst consequence: provenance for derived analytical artifacts should be event-structured rather than a single `derivedFrom` pointer, and full-fidelity audit export must be distinct from sanitized/publication export.

## Completed comparator: Automerge conflict semantics

Durable pass: `PASS-2026-09-11-forward-research-automerge-conflicts.md`.

Current core JS release found: 3.4.1 (2026-08-12). Current `automerge-repo` main reports 2.6.0-alpha.3; Keyhive/ARK access-control integration is explicitly still alpha.

Key result: CRDT convergence, mechanical edit collision, and intentional analytical disagreement are different states. Concurrent alternatives can be retained and inspected, but deterministic replica convergence is not human reconciliation.

Catalyst consequence: choose concurrency granularity by domain intent; preserve consequential conflicts visibly; model legitimate analyst disagreement as explicit authored perspectives; keep replica identity separate from authenticated human authorship.

## Forward-research sweep closed

`PASS-2026-09-11-forward-research-synthesis.md` now incorporates Hypothesis anchoring, OpenRefine provenance, and Automerge collaboration into the earlier AFFiNE/Logseq/Excalidraw/Zettlr/SilverBullet synthesis.

No further comparator should be added merely for breadth. Resume forward research only if architecture/prototyping identifies a concrete mechanism that remains weakly evidenced. Next handoff target is architecture synthesis/prototype constraints, not another generic note/canvas tool.
