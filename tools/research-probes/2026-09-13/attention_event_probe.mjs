const pages=await fetch('http://127.0.0.1:9229/json').then(r=>r.json());const page=pages.find(x=>x.type==='page'&&x.url.includes('127.0.0.1:5173'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise(r=>ws.onopen=r);const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async expression=>(await c('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true})).result.result.value;
const result=await v(`(()=>{const n=[...document.querySelectorAll('.picture-occurrence')].find(x=>x.querySelector('.picture-object-label')?.textContent==='Key assumption');const r=n.getBoundingClientRect();const hit=document.elementFromPoint(r.left+r.width/2,r.top+r.height/2);n.dispatchEvent(new MouseEvent('mouseover',{bubbles:true,view:window}));return{rect:{x:r.x,y:r.y,w:r.width,h:r.height},hit:hit?.className||hit?.tagName,hovered:n.matches(':hover')}})()`);await new Promise(r=>setTimeout(r,350));const state=await v(`({dim:document.querySelectorAll('.picture-occurrence.is-dimmed').length,semantic:document.querySelectorAll('.picture-semantic-relation').length})`);console.log(JSON.stringify({result,state},null,2));ws.close();
