const pages=await fetch('http://127.0.0.1:9231/json').then(r=>r.json());const page=pages.find(p=>p.type==='page'&&p.url.includes('run=file-e2e'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const w=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&w.has(m.id)){w.get(m.id)(m);w.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;w.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async e=>(await c('Runtime.evaluate',{expression:e,returnByValue:true})).result.result.value;const out=await v(`(()=>[...document.querySelectorAll('input,textarea,[contenteditable=true]')].map((e,i)=>{const r=e.getBoundingClientRect();return{i,tag:e.tagName,cls:e.className,aria:e.getAttribute('aria-label')||'',placeholder:e.getAttribute('placeholder')||'',value:e.value||e.textContent||'',x:r.x,y:r.y,w:r.width,h:r.height,visible:r.width>0&&r.height>0}}).filter(x=>x.visible))()`);console.log(JSON.stringify(out,null,2));ws.close();