const pages=await fetch('http://127.0.0.1:9229/json').then(r=>r.json()); const page=pages.find(x=>x.type==='page'); const ws=new WebSocket(page.webSocketDebuggerUrl); let id=1; const p=new Map(); ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}}; await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j}); const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))}); const r=await c('Runtime.evaluate',{expression:`(()=>{const s=JSON.parse(localStorage.getItem('catalyst.browser.workspace.v1'));const m=s.mapView.maps[s.mapView.activeMapId];return {active:s.activeNoteId,relationships:Object.keys(s.relationships||{}).length,occurrences:Object.fromEntries(Object.entries(m.occurrences).map(([id,o])=>[id,{parent:o.parentNoteId,order:o.siblingOrder,manual:o.manual,position:o.position}]))}})()`,returnByValue:true}); console.log(JSON.stringify(r.result.result.value,null,2)); ws.close();