const [xs,ys,...rest]=process.argv.slice(2);const x=Number(xs),y=Number(ys),text=rest.join(' ');const pages=await fetch('http://127.0.0.1:9231/json').then(r=>r.json());const page=pages.find(p=>p.type==='page'&&p.url.includes('run=file-e2e'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const w=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&w.has(m.id)){w.get(m.id)(m);w.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;w.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});for(const type of ['mousePressed','mouseReleased'])await c('Input.dispatchMouseEvent',{type,x,y,button:'left',clickCount:1});await c('Input.insertText',{text});console.log(`typed ${text.length} chars at ${x},${y}`);ws.close();