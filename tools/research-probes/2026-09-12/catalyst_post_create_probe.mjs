const pages=await fetch('http://127.0.0.1:9229/json').then(r=>r.json());const page=pages.find(p=>p.type==='page'&&p.url.includes('run=shell-experiment'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async e=>(await c('Runtime.evaluate',{expression:e,returnByValue:true,awaitPromise:true})).result.result.value;await c('Emulation.setDeviceMetricsOverride',{width:1440,height:900,deviceScaleFactor:1,mobile:false});
const clicked=await v(`(()=>{const b=document.querySelector('[aria-label="Create linked thought"]');if(!b)return false;b.click();return true})()`);await new Promise(r=>setTimeout(r,1200));
const out=await v(`(()=>{const rr=e=>{if(!e)return null;const r=e.getBoundingClientRect();return{x:r.x,y:r.y,w:r.width,h:r.height}};return{tabs:[...document.querySelectorAll('[role=tab]')].map(x=>x.textContent.trim()),title:document.querySelector('.note-title-input')?.value??null,body:document.querySelector('.note-body-input')?.value??null,occ:[...document.querySelectorAll('.picture-occurrence')].map(e=>rr(e)),sourceJump:rr(document.querySelector('.source-jump')),selection:!!document.querySelector('.selection-card'),workspace:document.querySelector('.workspace')?.className}})()`);console.log(JSON.stringify(out,null,2));ws.close();