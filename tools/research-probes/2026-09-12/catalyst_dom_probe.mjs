const pages=await fetch('http://127.0.0.1:9229/json').then(r=>r.json());const page=pages.find(p=>p.type==='page'&&p.url.includes('run=shell-experiment'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async e=>(await c('Runtime.evaluate',{expression:e,returnByValue:true})).result.result.value;await c('Emulation.setDeviceMetricsOverride',{width:1440,height:900,deviceScaleFactor:1,mobile:false});
const out=await v(`(()=>{const root=document.querySelector('.reader-surface');const a=[];const walk=(node,d=0)=>{if(!node||d>8||a.length>400)return;for(const e of node.children||[]){const r=e.getBoundingClientRect();a.push({d,tag:e.tagName,cls:String(e.className).slice(0,160),id:e.id||'',role:e.getAttribute?.('role')||'',aria:e.getAttribute?.('aria-label')||'',r:{x:r.x,y:r.y,w:r.width,h:r.height},shadow:!!e.shadowRoot});if(e.shadowRoot)walk(e.shadowRoot,d+1);walk(e,d+1)}};walk(root);return a})()`);console.log(JSON.stringify(out,null,2));ws.close();