const ps=await fetch("http://127.0.0.1:9229/json").then(r=>r.json());const page=ps.find(x=>x.type==="page");const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async expression=>(await c("Runtime.evaluate",{expression,returnByValue:true})).result.result.value;
console.log(JSON.stringify(await v(`(()=>{const sels=['.workspace','.context-pane','.working-picture-workspace','.working-picture-viewport','.pdf-pane','.note-inspector','.context-header','.context-tabs'];const out={};for(const s of sels){const e=document.querySelector(s);if(!e){out[s]=null;continue;}const r=e.getBoundingClientRect();out[s]={x:r.x,y:r.y,w:r.width,h:r.height,display:getComputedStyle(e).display,position:getComputedStyle(e).position,z:getComputedStyle(e).zIndex};}return out})()`),null,2));ws.close();