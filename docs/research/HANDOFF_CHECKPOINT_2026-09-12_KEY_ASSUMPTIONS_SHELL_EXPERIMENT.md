# Catalyst handoff checkpoint — key assumptions / shell experiment

Date: 2026-09-12
Reason: current chat is beginning to lag; deliberate handoff before instability.

## Read first
1. `docs\research\KEY_ASSUMPTIONS_CHECK_2026-09-12.md`
2. `docs\research\RESEARCH_CLOSURE_SYNTHESIS_2026-09-12.md`
3. `docs\research\HANDOFF_CHECKPOINT_2026-09-12_LAYERED_WORKFLOW.md`
4. Current `src\App.tsx`, `src\styles.css`, `src\features\selection\SelectionCard.tsx`, and Working Picture placement files.

## Authoritative directional correction
Do NOT assume the literal continuous-page / floating-PDF shell is the product direction.
The key-assumptions check found that the stable requirement is **continuous analytical context**, not necessarily one continuous visual page.
The Working Picture remains a first-class spatial synthesis/foraging projection, but not necessarily the permanent shell or ontology.

Stable invariants that survived: exact provenance/source return; separation of identity, occurrence, structure, semantics, provenance, focus and portrayal; version-aware state; local-first/non-proprietary operation; recoverable complexity; optional intelligence tradecraft rigor without making the product intelligence-only.

Downgraded hypotheses: one literal page, Working Picture as permanent home, mind-map/spatial organization as fundamental note structure, PDF overlap as default composition, and immediate classification of every source selection as `Evidence`.## Real-file diagnostic already completed
A clean isolated Catalyst session loaded `C:\Users\iris\Downloads\Tradecraft-Primer-apr09.pdf` through the real file input with zero fixture state.
Observed path: `Working picture 0 / Evidence 0` → real PDF selection → `Save evidence` produced Evidence 1 → same selection + `Create linked thought` produced Working picture 1 and Evidence 2.

Important findings:
- initial unified-page PDF scale was too small for comfortable reading;
- `Fit to Width` was below the visible EmbedPDF popup at the tested viewport, and repeated zoom required reopening the menu each time;
- source text selection itself worked cleanly;
- EmbedPDF's own annotation popover appeared at the selection locus, while Catalyst's `Create linked thought / Highlight source / Save evidence / Copy with source` toolbar appeared elsewhere;
- therefore source→analysis capability exists, but the handoff is spatially/discoverably weak;
- `createNoteFromSelection()` currently always creates a fresh annotation UUID before creating/linking a note, so Evidence 1 → 2 for the same source selection is genuine duplication behavior, not a display artifact;
- this supports the hypothesis that durable source-region/excerpt identity should be reusable, while `evidence` is an analytical role/use rather than a necessarily cloned object.

The linked thought was titled `Structured analytic techniques improve intelligence analysis` with a short analytical body and retained the Tradecraft p.1 source link. Do not treat that content as fixture architecture; it was only diagnostic material.

## Current implementation state
The earlier placement/stacking slice passed focused placement tests, full `npm test`, and production build. Live Opera validation proved actual z-order inversion: source-above PDF z=50/context z=20; analysis-above PDF z=30/context z=40; both remain pointer-events auto. Renderer F6 works when delivered, but Chromium/Opera owns F6 at browser-chrome level, so it is not a portable web accelerator. Keep this work as the spatial-first control branch; do not discard it.## Exact next experiment
Do NOT restart broad research and do NOT repair the current literal-page shell first.
Build only research-only shell variants over the same existing state model and Tradecraft task:

1. **Spatial-first control** — current `?research=unified-workflow` layered/floating-PDF behavior, unchanged except only if needed to keep it runnable.
2. **Reader-first** — readable source dominates; Catalyst capture bridge is locally prominent at/near selection; analytical projection opens without losing deterministic source context.
3. **Split/focus workbench** — source and analytical projection are independently focusable/resizable regions; simultaneity is available but overlap is not required.

Prefer URL research switches such as `shell=spatial|reader|split`; keep all three on the same commands/state so the independent variable is composition.

Run the identical task in each: open real Tradecraft PDF → select source region → create grounded thought → organize/place it → return exactly to source → resume analysis.
Record source readability, interaction steps/friction, source→thought discoverability, exact return, interruption/reorientation, provisional organization, source-vs-interpretation clarity, and any duplicate/categorization errors.

Pivot criterion: if reader-first or split/focus materially outperforms spatial-first without harming synthesis/reorientation, pivot the shell while retaining Working Picture as a first-class projection. If spatial-first wins after fair correction, retain it on evidence.

## Scope / continuity
User explicitly said coding can resume after this check, but this three-shell experiment comes first.
User also suggested Sploder pixel-editor logic may later help analyst-authored visual-model customization; keep it as portrayal-layer research, not core shell direction.
Do not re-enable the autonomous watchdog/continue system. Manual chat handoff is authorized. New chats, bridge-extension edits/reloads, and local plugins are allowed if useful; no APIs are desired.
Use Remote Desktop Commander for local work. Prefer coherent tested slices and preserve the current spatial work as an experimental control rather than deleting it.## Mandatory cleanup before coding
Before editing Catalyst for the shell experiment, clean up the loose test/probe artifacts created in `C:\Users\iris\Downloads` during this chat.

Inventory first; do not blindly delete or move unrelated user downloads. The generated Catalyst diagnostics include files such as `catalyst_file_e2e_stage1.mjs`, `catalyst_pdf_probe.mjs`, `catalyst_cdp_screenshot.mjs`, `catalyst_controls_probe.mjs`, `catalyst_click_aria.mjs`, `catalyst_click_text.mjs`, `catalyst_drag.mjs`, `catalyst_interactives.mjs`, `catalyst_click_xy.mjs`, `catalyst_fields.mjs`, and `catalyst_type_xy.mjs` (plus any similarly named temporary probes from this pass).

Create/use an appropriate Catalyst-owned research/debug location, e.g. `C:\Users\iris\Downloads\Catalyst\tools\research-probes\2026-09-12\`, and move useful reproducible probes there. Delete only clearly disposable generated artifacts after verifying they are redundant. Keep user-provided/source material such as `Tradecraft-Primer-apr09.pdf` and `pixeleditor.swf` where the user put them unless there is an explicit reason and permission to reorganize those too.

Also inspect `C:\Users\iris\Downloads\Catalyst\tmp` for generated screenshots/probe outputs from this pass and either retain them deliberately under the project or remove disposable copies. Record what was moved/deleted in the successor checkpoint before coding.## Successor chat
Manual Catalyst Chat Bridge rollover launched successfully after this checkpoint was written.

Title observed in Opera history: `Plan Cleanup And Shell Experiment`
URL: `https://chatgpt.com/c/6aa593b7-f810-83ea-82b6-a555bbbbf786`

The bootstrap explicitly instructs the successor to read this checkpoint first, clean up loose Catalyst probe/test artifacts in Downloads before coding, preserve user source files, then implement and compare the three research-only shell variants. Autonomous watchdog remains disabled.