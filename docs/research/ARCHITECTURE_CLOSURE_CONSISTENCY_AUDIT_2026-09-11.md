# Architecture closure consistency audit — 2026-09-11

Status: documentation consistency audit only. No GUI manipulation and no product code.

## Scope

Reviewed the durable high-level decision/state documents most likely to guide a later implementer:
- `DECISION_LOG.md`
- `NEXT_STEPS.md`
- `CURRENT_STATE.md`
- `RESEARCH_SYNTHESIS.md`
- `ROLLOVER_CHECKPOINT_2026-09-11B.md`
- current Working Picture architecture contract/handoff.

The purpose was to find stale language that could accidentally revive a graph-first, universal-tree, infinite-canvas, or permanent Reader+canvas architecture after the newer research closure.

## Result

No current high-level decision directly contradicts the normative Working Picture architecture contract.

The durable decision log explicitly records:
- graph as computational infrastructure rather than primary product metaphor;
- bounded analytical desk rather than infinite canvas/graph;
- source identity spanning landmark, Quick Look/open reading representation, and exact return;
- mind-map branching as first-class but not universal/lowest-commitment representation;
- separate identity, occurrence, structure, semantic, provenance, and focus scopes.

`CURRENT_STATE.md` and the latest sections of `RESEARCH_SYNTHESIS.md` agree with those decisions.

## Historical language that is safe only with precedence understood

`NEXT_STEPS.md` begins with an older Portrayal-Lab-first phase sequence. Later sections explicitly supersede that ordering twice and make cognitive architecture plus a disposable interaction prototype the current gate.

Interpretation: the older numbered phases are historical planning, not the current execution order. Do not start production alpha.4 or treat Portrayal Lab completion as the immediate blocker merely because those sections appear first in the file.

`ROLLOVER_CHECKPOINT_2026-09-11B.md` says to continue research until the cognitive architecture is defensible. That condition has now been met by later research artifacts. Under the coordination file's source-of-truth order, the current architecture contract, state, and coordinator handoff supersede that older open-ended research instruction.

`RESEARCH_SYNTHESIS.md` still contains older shorthand such as `Open: exact Reader/source location`. Later sections explicitly correct the conceptual model: Reader is not a separate universe; opening a source is a focused representation of the same placed source identity with return-to-context.

## Potentially ambiguous legacy term: Map

`DECISION_LOG.md` and `CURRENT_STATE.md` retain language such as `Map / portrayal state` beneath the Working Picture.

This remains compatible only if `Map` is read as saved/contextual portrayal state, not as:
- a second canonical analytical ontology;
- one global coordinate set per identity;
- mandatory tree/graph structure;
- or a duplicate set of analytical objects.

The newer term `Working Picture/context-local occurrence and portrayal state` is more precise for implementation discussions.

## Implementation-facing precedence after closure

For Working Picture cognitive architecture, use this order when wording differs:

1. `WORKING_PICTURE_ARCHITECTURE_CONTRACT_2026-09-11.md`
2. `PASS-2026-09-11-working-picture-prototype-acceptance-contract.md`
3. `WORKING_PICTURE_PROTOTYPE_OBSERVABILITY_2026-09-11.md`
4. latest `ARCHITECTURE_CHECKPOINT_2026-09-11.md`
5. `DECISION_LOG.md` / latest `CURRENT_STATE.md`
6. older synthesis, next-step, and rollover material as historical rationale.

This is subordinate to the global environment/coordination source-of-truth order for workbench safety and live process state.

## Closure recommendation

Do not spend another research cycle rewriting historical documents merely to erase their chronology. The supersession trail is useful evidence of why Catalyst changed direction.

Instead, treat the compact architecture contract plus evaluator/observability contracts as the implementation-facing gate. Reconcile historical language only when it creates a concrete implementation ambiguity.

The closure audit therefore finds no documentation contradiction that blocks deliberate research closure or disposable prototyping.
