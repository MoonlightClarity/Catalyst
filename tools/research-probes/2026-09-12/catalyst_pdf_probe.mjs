const pages=await fetch('http://127.0.0.1:9231/json').then(r=>r.json()); const page=pages.find(p=>p.type==='page'&&p.url.includes('run=file-e2e')); const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const v=async e=>(await c('Runtime.evaluate',{expression:e,returnByValue:true})).result.result.value;
const out=await v(`(()=>{const all=[];const walk=root=>{for(const e of root.querySelectorAll('*')){const s=(e.className&&String(e.className))||'';const a=e.getAttribute?.('aria-label')||'';if(/text|select|annot|page|pdf/i.test(s+' '+a+' '+e.tagName)) all.push({tag:e.tagName,cls:s.slice(0,100),aria:a,text:(e.textContent||'').trim().slice(0,80)});if(e.shadowRoot)walk(e.shadowRoot)}};walk(document);return all.slice(0,250)})()`);console.log(JSON.stringify(out,null,2));ws.close();