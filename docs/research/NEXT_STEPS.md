# Recommended next steps

## Phase 1 — Portrayal Lab, before alpha.4

1. Create a dedicated Portrayal Lab route/page/tool outside the normal Catalyst Working Picture, backed by a versioned portrayal catalog rather than loose SVG files.
2. Establish portrayal-rule precedence, construction primitives, modifier zones/budget, and size rules before final core symbols.
3. Create several candidate core-symbol families with varying concreteness.
4. Add modifier/state composition using fixed semantic zones.
5. Author distinct 16/24/32/48 px variants.
6. Implement light/dark/monochrome specimens.
7. Create point/line/area/branch/trace/junction/territory examples.
8. Build the five evaluation stations described in `PORTRAYAL_EXECUTION.md`, record structured test results/confusions, and use a fixed Working-Picture test corpus.
9. Iterate until basic category recognition/discrimination is reliable enough for product integration.

## Phase 2 — Native brand/icon plumbing

In parallel with the Lab, repair native metadata:

1. synchronize Tauri/Cargo/release versions;
2. explicitly configure bundle icons;
3. explicitly set/verify the development window icon path;
4. create app-mark small-size variants from the validated visual grammar;
5. add an acceptance check for the native taskbar/title icon.

## Phase 3 — License validation repair

1. Separate “license problem found” from “audit could not execute.”
2. Compare dependency manifests/locks against the validated baseline.
3. Only require ecosystem-specific re-audit when dependencies changed, unless explicitly running full audit mode.
4. Preserve conservative failure behavior when dependency contents really did change.

## Phase 4 — Working Picture alpha.4

Only after portrayal components pass the Lab:

1. replace alpha.3 core symbols with validated landmarks;
2. render real source crops as first-class visual objects;
3. reduce inspector footprint for ordinary Inspect;
4. show structural branches as composition, not graph edges;
5. keep semantic relationships latent until requested/relevant;
6. introduce real point/line/area portrayal primitives;
7. use aggressive level-specific semantic zoom;
8. preserve analyst-authored spatial landmarks;
9. retain Back/Home/Focus/Open navigation grammar;
10. test with runtime screenshot and task-based navigation before adding new analytical features.

## Phase 5 — Resume analytical architecture

Once the visual/navigation foundation proves itself, continue the previously researched domain sequence toward:

- evidence provenance/source families;
- richer analytical propositions/inference;
- assessments/perspectives;
- meaningful semantic event history;
- indicators/Watch/Scan;
- projection views such as timeline/matrix/method-specific displays;
- briefing/product composition.

Do not implement these rich semantics merely to create more things for alpha.4 to display.

## Phase 0 — canonical-source reconciliation before alpha.4 packaging

Do not interrupt the portrayal research to fix these immediately, but before alpha.4 handoff:

1. reconcile/remove stale root-level research copies after verifying `docs/research/` is canonical;
2. reconcile `specs/` against `docs/` and either archive it explicitly or remove redundant copies;
3. make clean/gather validation reject leftover transport artifacts;
4. synchronize package/Tauri/Cargo/release versions;
5. explicitly configure and test the runtime/bundle app icon.

## Phase 1A — executable Portrayal Lab architecture

Before drawing final symbols:

1. create `src/portrayal/` catalog/context/resolver/primitives/test scaffolding;
2. implement `explainPortrayal()` so rule resolution is inspectable;
3. author fixed P1–P8 benchmark scenes from `PASS-2026-09-11-portrayal-lab-execution.md`;
4. implement test-result persistence and confusion summaries;
5. only then begin candidate core-symbol families.

## Priority override — cognitive workspace architecture before next production rewrite

The rollover review and cognitive-workspace research supersede the earlier assumption that Portrayal Lab completion is the immediate product blocker. Portrayal work remains valid infrastructure, but the next production Working Picture must first have a tested cognitive architecture.

Before more production UI coding:

1. Build a disposable interaction study/mock prototype for the bounded analytical desk.
2. Test the commitment ladder: capture → placement → organization → semantic assertion.
3. Restore source/document placement as a first-class operation, including exact source-region return.
4. Test a low-ceremony staging pattern for incoming/unplaced material.
5. Test free clusters/piles/territories that carry no automatic semantic relation.
6. Test promotion of provisional layout into structural branches/containers without destroying the original geography.
7. Test lightweight selection versus deliberate Inspect/Open.
8. Test subpicture navigation and Back/Home return with stable spatial memory.
9. Test semantic zoom/spread/compress behavior without automatic landmark reflow.
10. Only after the interaction model works, combine it with the validated portrayal catalog/primitives and resume the production Working Picture rewrite.

Evaluation should measure whether the analyst can begin useful work before learning Catalyst's taxonomy, whether source-to-analysis trace remains obvious, and whether the interface itself recedes from attention.
## Current priority override — cognitive architecture before next Working Picture code

The earlier Portrayal-Lab-first sequence is now historically superseded as the immediate priority. Blind-v1/P7 is validated far enough to expose the larger UI problem; blind-v2 was not applied.

Before any next Working Picture implementation:

1. Compare reduced split-pane, literal spatial desk, and anchored-material + bounded-subpicture architectures.
2. Use one realistic multi-source investigation to trace the complete interaction sequence from source intake through provisional grouping to one explicit analytical relationship.
3. Evaluate the alternatives with Cognitive Dimensions: premature commitment, provisionality, secondary notation, visibility/juxtaposability, viscosity, hidden dependencies, role-expressiveness, closeness of mapping, hard mental operations, and progressive evaluation.
4. Require structural-legibility, source-return, spatial-truth, chrome-retreat, and density tests.
5. Preserve exact source-region anchors and one analytical identity across multiple picture occurrences/portrayal levels.
6. Keep application chrome subordinate to the analytical surface and avoid adding permanent panes to solve discoverability.
7. Only after the cognitive architecture is selected should portrayal integration and Working Picture code resume.

See `PASS-2026-09-11-cognitive-architecture-ui.md`.

## Latest architecture gate — 2026-09-11

The earlier comparison phase is now substantially complete. The research has converged on the bounded analytical desk / focus-in-context / bounded-subpicture direction and has been translated into a state-separation contract.

Before any production Working Picture implementation, the coordinator should deliberately close the research pass and use `WORKING_PICTURE_ARCHITECTURE_CONTRACT_2026-09-11.md` as the gate for a **disposable interaction prototype**.

That prototype should test the ten-step acceptance sequence in the contract: free source placement; anchored region capture/return; same-identity reuse; context-local organization; independent movement; explicit semantic assertion; connector portrayal independence; provenance-preserving derivation; focus/subpicture return; and occurrence removal without identity deletion.

The prototype may use temporary storage, but it must preserve the mutation/destructive-operation discipline. Do not resume production renderer work merely because the surface mockup looks calmer.

Further generic comparator research is not required for this gate. Multi-user conflict semantics is a distinct later architecture question; completed Trilium GUI evidence should be reconciled if/when it appears.
### Prototype handoff artifact hierarchy

For the disposable cognitive prototype, use `WORKING_PICTURE_ARCHITECTURE_CONTRACT_2026-09-11.md` as the compact normative invariant checklist and `PASS-2026-09-11-working-picture-prototype-acceptance-contract.md` as the 12-step evaluator scenario.

The two documents are complementary: the first says what state/command boundaries must hold; the second demonstrates how to falsify them in one realistic multi-source investigation. Detailed deletion/projection rules live in `PASS-2026-09-11-architecture-state-ownership-destruction-projections.md`.

## Research stop condition — 2026-09-11

The forward comparator sweep is closed and architecture has incorporated the three previously narrow gaps: version-aware anchoring, event-structured transformation provenance, and collaboration/conflict semantics.

Do not add another generic comparator before the disposable Working Picture prototype. New research is justified only if prototype review exposes a specific mechanism that remains uncertain or if an authorized Trilium hands-on pass produces a genuine counterexample.

The coordinator/user may now deliberately close the cognitive-architecture research phase and authorize the disposable single-user prototype against the compact architecture contract plus 12-step acceptance scenario. Collaboration remains a future-compatibility constraint, not immediate prototype scope.

## Phase transition — disposable interaction prototype (2026-09-12)

Broad cognitive-architecture research is deliberately closed. Do not resume generic comparator research before the disposable prototype produces a specific counterexample.

Immediate prototype sequence:
1. Refactor the existing `#desk-prototype` route so durable identity and occurrence/placement are separate in-memory species.
2. Route consequential prototype actions through named commands with declared mutation species and a visible mutation trace.
3. Add same-identity `Reuse`, explicit `Fork`, occurrence-only removal, structure membership, context-local role, and qualified semantic assertion as distinct operations.
4. Preserve connector portrayal as separate from semantic assertion truth.
5. Add bounded subpicture focus/return plus quiet outside-picture coverage cues.
6. Add one non-spatial synchronized projection for recovery/accessibility testing.
7. Exercise source-region identity/representation/anchor resolution with the existing EmbedPDF stack only after the state-boundary loop is observable.
8. Run the fixed multi-source acceptance scenario and adversarial tests; preserve every genuine counterexample.

This is research instrumentation, not alpha.4. Visual polish, database-schema commitment, collaboration implementation, and production migration remain out of scope until the prototype passes its architecture and cognitive gates.

## Latest execution-order override — preliminary project roadmap (2026-09-13)

The active project state has moved beyond the older Portrayal-Lab-first and disposable-prototype-only sequencing. Those passes remain valid evidence, but current product work has already entered implementation and integration across source-first UI, node simplification, Freeplane structure, Thorium reader architecture, SVG curation, persistence experiments, and code audit tracks.

Use `PRELIMINARY_PROJECT_ROADMAP_2026-09-13.md` as the current execution-order snapshot.

Immediate order:
1. Reconcile parallel edits into a known-good integration checkpoint.
2. Establish the source-first Functional Baseline: read -> select -> capture -> evidence/thought -> analysis -> exact source return.
3. Finish simplifying the Analysis node/runtime boundaries before expanding structural behavior.
4. Fill structural interaction gaps against Freeplane rather than inventing new basic map mechanics.
5. Refactor Reader/annotation behavior against Thorium while preserving Catalyst source/evidence semantics.
6. Consolidate SVG/color/typography/native-brand work only after ordinary workflow usability is visually validated.
7. Treat XML persistence as the active runtime baseline; keep any future desktop-shell work separate from persistence authority and preserve the archived Tauri shell only as legacy reference.
8. Resume deeper analytical semantics and projection views on top of the stable functional baseline.

The near-term milestone is **Catalyst Functional Baseline**, not alpha.4 for its own sake.