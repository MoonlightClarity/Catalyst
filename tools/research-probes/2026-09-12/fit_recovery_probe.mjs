const pages=await fetch('http://127.0.0.1:9229/json').then(r=>r.json());const page=pages.find(p=>p.type==='page');const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1,p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(m,q={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method:m,params:q}))});const v=async e=>(await c('Runtime.evaluate',{expression:e,returnByValue:true,awaitPromise:true})).result.result.value;const before=await v(`(()=>[...document.querySelectorAll('.picture-occurrence')].map(e=>{const r=e.getBoundingClientRect();return{x:r.x,y:r.y,w:r.width,h:r.height}}))()`);await v(`document.querySelector('[aria-label="Fit picture"]')?.click();true`);await new Promise(r=>setTimeout(r,600));const after=await v(`(()=>[...document.querySelectorAll('.picture-occurrence')].map(e=>{const r=e.getBoundingClientRect();return{x:r.x,y:r.y,w:r.width,h:r.height}}))()`);console.log(JSON.stringify({before,after},null,2));ws.close();