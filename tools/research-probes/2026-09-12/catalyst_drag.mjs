const [sx,sy,ex,ey]=process.argv.slice(2).map(Number);const pages=await fetch('http://127.0.0.1:9231/json').then(r=>r.json());const page=pages.find(p=>p.type==='page'&&p.url.includes('run=file-e2e'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=1;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise(r=>{const n=id++;p.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});await c('Input.dispatchMouseEvent',{type:'mouseMoved',x:sx,y:sy});await c('Input.dispatchMouseEvent',{type:'mousePressed',x:sx,y:sy,button:'left',clickCount:1});for(let i=1;i<=12;i++){await c('Input.dispatchMouseEvent',{type:'mouseMoved',x:sx+(ex-sx)*i/12,y:sy+(ey-sy)*i/12,button:'left',buttons:1});await new Promise(r=>setTimeout(r,25));}await c('Input.dispatchMouseEvent',{type:'mouseReleased',x:ex,y:ey,button:'left',clickCount:1});await new Promise(r=>setTimeout(r,600));console.log('dragged');ws.close();